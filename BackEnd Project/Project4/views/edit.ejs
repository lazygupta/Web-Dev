<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-5">
            <a class="text-blue-500" href="/read" class="">Show All Users</a>
        </div>
        <h1 class="text-3xl tracking-lighter mb-3">Update User</h1>
        <form action="/update/<%= user._id %>" method="POST">
            <label for="name" class="">Name</label>
            <input name="name" type="text" class="px-2 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none text-zinc-500" placeholder="<%= user.name %>" value="<%= user.name %>">

            <label for="email" class="">Email</label>
            <input name="email" type="text" class="px-2 py-2 bg-transparent border-2 text-zinc-500  border-zinc-800 rounded-lg outline-none" placeholder="<%= user.email %>" value="<%= user.email%>">

            <div class="w-52 h-52 bg-zinc-700 rounded-lg overflow-hidden mt-2">
                <img src="<%= user.image %>" alt="" class="w-full h-full object-cover ">
            </div>

            <label for="image" class="">Image URL</label>

            <input name="image" type="text" class="px-2 py-2 bg-transparent border-2 text-zinc-500 border-zinc-800 rounded-lg outline-none" placeholder="image url" value="<%= user.image %>" >
            
            <input type="submit" class="px-5 py-2 bg-yellow-500 rounded-lg mt-5 text-black" value="Update User">
        </form>
    </div>
</body>
</html>